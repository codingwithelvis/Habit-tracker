name: Build APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the code
        uses: actions/checkout@v3  # Updated to v3 to avoid Node.js 12, 16, and use Node.js 20

      - name: Set up Python
        uses: actions/setup-python@v4  # Updated to v4 to be compatible with Node.js 20
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip
          pip install --upgrade cython
          pip install buildozer

      - name: Install Android SDK and NDK
        run: |
          sudo apt-get install -y android-sdk
          sudo apt-get install -y android-ndk

      - name: Build APK with Buildozer
        run: |
          buildozer android debug
          
      - name: Upload APK artifact
        uses: actions/upload-artifact@v3  # Already up-to-date to run on Node.js 20
        with:
          name: MyApp.apk
          path: bin/*
